/// <reference types="vite/client" />

// Injected by Vite (see `define` in vite.config.ts) from package.json at
// build time. Surfaced in the Settings → About panel.
declare const __APP_VERSION__: string;
